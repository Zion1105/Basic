this is for testing
is it working
/-------------------------------------/
git init
git add .
git commit -m "commitmessage"
git remote add origin {url}
git push -u origin {branchname}
git remote -v
git status
git log
/--------------------------------------/
git stash
git add . && git stash
git branch -a
git checkout -b (for new branch)
git checkout master
git merge users